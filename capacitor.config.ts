import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gandiKahaniya',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
